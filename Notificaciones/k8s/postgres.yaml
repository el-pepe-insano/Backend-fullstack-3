apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-notificaciones-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-notificaciones-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-notificaciones
  template:
    metadata:
      labels:
        app: postgres-notificaciones
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: Bullseye
            - name: POSTGRES_DB
              value: godofgames_notificaciones
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-notificaciones-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-notificaciones-service
spec:
  selector:
    app: postgres-notificaciones
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432